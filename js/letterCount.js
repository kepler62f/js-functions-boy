//use objects to count
//split() string into array of characters
//define object "occurrence" with elements of character array
// letter = strarr[i]
// occurrence[letter]
// if occurrence undefined, count as 1 else count +1
